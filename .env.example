# App
APP_NAME="Dieter HQ"
APP_BASE_URL="http://localhost:3000"

# Auth (temporary)
HQ_PASSWORD=""

# Database (Neon Postgres recommended)
DATABASE_URL="postgresql://..."

# Web Push Notifications (VAPID)
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:your-email@example.com"

# OpenClaw Gateway Configuration
# ==================================
# There are TWO connection modes:
#
# 1. WebSocket (RECOMMENDED for real-time chat)
#    - Client-side connection, needs NEXT_PUBLIC_ prefix
#    - Supports streaming, events, real-time status
#
# 2. HTTP (fallback for server-side API calls)
#    - Server-side only, no NEXT_PUBLIC_ prefix
#    - Used for voice transcription, etc.

# WebSocket Mode (client-side, real-time chat)
NEXT_PUBLIC_OPENCLAW_WS_URL="ws://127.0.0.1:18789"
NEXT_PUBLIC_OPENCLAW_PASSWORD="your-gateway-password"

# HTTP Mode (server-side, API routes)
OPENCLAW_GATEWAY_HTTP_URL="http://127.0.0.1:18789"
OPENCLAW_GATEWAY_PASSWORD="your-gateway-password"

# Whisper transcription (local whisper-cpp server)
OPENCLAW_WHISPER_PORT="8082"

# Production URLs (via Tailscale Funnel)
# NEXT_PUBLIC_OPENCLAW_WS_URL="wss://your-machine.tailnet.ts.net"
# OPENCLAW_GATEWAY_HTTP_URL="https://your-machine.tailnet.ts.net"
