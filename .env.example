# ============================================
# Dieter HQ - Environment Variables
# ============================================
# Copy this to .env.local for local dev
# For Coolify: set these in the Coolify dashboard
# NEXT_PUBLIC_* vars must be set as build-time args in Coolify

# App
APP_NAME="Dieter HQ"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth
# Option 1: Plain text password (legacy, not recommended for production)
# HQ_PASSWORD=your-secret-password
#
# Option 2: Bcrypt hash (recommended for production)
# Generate with: node -e "require('bcrypt').hash('your-password', 12).then(console.log)"
# HQ_PASSWORD=$2b$12$...your-bcrypt-hash...
#
# If unset, auth is disabled (useful for local development)
HQ_PASSWORD=

# Database (Neon Postgres)
DATABASE_URL=postgresql://user:pass@host/dbname

# Web Push Notifications (generate with: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@your-domain.com
PUSH_API_KEY=

# OpenClaw Gateway
OPENCLAW_GATEWAY_URL=https://your-gateway.tailnet.ts.net
OPENCLAW_GATEWAY_HTTP_URL=https://your-gateway.tailnet.ts.net
OPENCLAW_GATEWAY_PASSWORD=
